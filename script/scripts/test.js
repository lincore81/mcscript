// This is a test file for the script mod!
mc.echo("Hello World!")
time = mc.time.get()
mc.echo("The time is: " + String(time))
if (time < 12000) {
	mc.echo("That means it is daytime.")
}
else {
	mc.echo("It's dark outside, be careful!")
}
mc.echo("Done! Any errors? No? Kewl!")
/*
 *  Yeah, we're done!
*/
